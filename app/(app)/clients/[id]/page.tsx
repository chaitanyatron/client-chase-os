import { notFound } from "next/navigation";
import Link from "next/link";
import { addDocumentRequests, updateDocumentStatus } from "@/app/actions";
import { requireUser } from "@/lib/auth";
import { effectiveStatus, waitingDuration, type Client, type DocumentRequest } from "@/lib/types";
import { StatusBadge } from "@/components/status-badge";

const suggestions = ["Bank Statement", "Sales Invoices", "Purchase Invoices", "GSTR-2B", "TDS Certificate", "Loan Statement", "Payroll Data", "Investment Proof"];

export default async function ClientDetailPage({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ error?: string }> }) {
  const { id } = await params; const { error } = await searchParams; const { supabase } = await requireUser();
  const { data: clientData } = await supabase.from("clients").select("*").eq("id", id).maybeSingle(); if (!clientData) notFound(); const client = clientData as Client;
  const { data: docsData } = await supabase.from("document_requests").select("*").eq("client_id", id).order("requested_at", { ascending: false }); const docs = (docsData || []) as DocumentRequest[];
  return <div><Link href="/clients" className="text-sm font-medium text-slate-600 hover:text-slate-950">← Back to clients</Link><div className="mt-5 flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><h1 className="text-3xl font-bold tracking-tight">{client.company_name || client.name}</h1><div className="mt-3 flex flex-wrap gap-x-5 gap-y-1 text-sm text-slate-600"><span>{client.email || "No email"}</span><span>{client.phone || "No phone"}</span></div></div></div><section className="mt-8 grid gap-6 lg:grid-cols-[minmax(0,1fr)_340px]"><div className="card overflow-hidden"><div className="border-b px-5 py-4"><h2 className="font-semibold">Documents required</h2><p className="mt-1 text-sm text-slate-500">Track every item you are waiting to receive.</p></div>{docs.length === 0 ? <div className="px-5 py-14 text-center"><h3 className="font-semibold">No document requests yet</h3><p className="mt-2 text-sm text-slate-500">Use the panel to add the documents you need from this client.</p></div> : <div className="divide-y">{docs.map(doc => { const status = effectiveStatus(doc); return <div key={doc.id} className="flex flex-col gap-3 px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-medium">{doc.document_name}</p><p className="mt-1 text-sm text-slate-500">Requested {new Intl.DateTimeFormat("en-IN", { dateStyle: "medium" }).format(new Date(doc.requested_at))}{doc.due_date ? ` · Due ${new Intl.DateTimeFormat("en-IN", { dateStyle: "medium" }).format(new Date(doc.due_date + "T12:00:00"))}` : ""}{status !== "received" ? ` · ${waitingDuration(doc.requested_at)}` : doc.received_at ? ` · Received ${new Intl.DateTimeFormat("en-IN", { dateStyle: "medium" }).format(new Date(doc.received_at))}` : ""}</p></div><div className="flex items-center gap-2"><StatusBadge status={status}/>{status !== "received" && <form action={updateDocumentStatus}><input type="hidden" name="client_id" value={id}/><input type="hidden" name="request_id" value={doc.id}/><input type="hidden" name="status" value="received"/><button className="button-secondary !px-3 !py-1.5">Mark received</button></form>}{status === "pending" && <form action={updateDocumentStatus}><input type="hidden" name="client_id" value={id}/><input type="hidden" name="request_id" value={doc.id}/><input type="hidden" name="status" value="overdue"/><button className="text-xs font-semibold text-rose-600 hover:text-rose-700">Mark overdue</button></form>}</div></div>; })}</div>}</div><aside className="card h-fit p-5"><h2 className="font-semibold">Add document requests</h2><p className="mt-1 text-sm text-slate-500">Select one or more items to request.</p>{error && <p className="mt-4 rounded-lg bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<form action={addDocumentRequests} className="mt-5"><input type="hidden" name="client_id" value={id}/><fieldset className="space-y-2.5">{suggestions.map(item => <label key={item} className="flex cursor-pointer items-center gap-2.5 text-sm text-slate-700"><input name="document_names" value={item} type="checkbox" className="h-4 w-4 rounded border-slate-300 text-indigo-600 focus:ring-indigo-500"/>{item}</label>)}<label className="flex items-center gap-2.5 text-sm text-slate-700"><input name="document_names" value="Other" type="checkbox" className="h-4 w-4 rounded border-slate-300 text-indigo-600 focus:ring-indigo-500"/>Other</label></fieldset><label className="label mt-5">Due date <span className="font-normal text-slate-400">(optional)</span><input type="date" name="due_date" className="field" /></label><button className="button-primary mt-5 w-full">Add selected documents</button></form></aside></section></div>;
}
